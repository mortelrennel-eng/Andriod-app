<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ✅ Feature requirement -->
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <!-- ✅ Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Admin & Super Admin Features -->
        <activity android:name=".AdminDashboard" />
        <activity android:name=".SuperAdminDashboard" />
        <activity android:name=".RegisterAdminActivity" />
        <activity android:name=".RegisterSuperAdminActivity" />
        <activity android:name=".AdminRegistrationCompleteActivity" />
        <activity android:name=".UsersListActivity" />
        <activity android:name=".ManageStudentsActivity" />
        <activity android:name=".ManageSectionsActivity" />
        <activity android:name=".AddEbookActivity" />
        <activity android:name=".EbookManagerActivity" />
        <activity android:name=".ViewAdminsActivity" />
        <activity android:name=".QRGeneratorActivity" />
        <activity android:name=".ManageAttendanceActivity" />
        <activity android:name=".CreateAnnouncementActivity" />
        <activity android:name=".ManageAdminsActivity" />
        <activity android:name=".SectionAttendanceActivity" />
        <activity android:name=".AddStudentsToSectionActivity" />
        <activity android:name=".ViewStudentsInSectionActivity" />
        <activity android:name=".ViewAttendanceActivity" />
        <activity android:name=".StudentListBySectionActivity" />
        <activity android:name=".AttendanceListBySectionActivity" />
        <activity android:name=".EditStudentActivity" />
        <activity android:name=".StudentListForAttendanceActivity" /> <!-- NEW -->
        <activity android:name=".AttendanceHistoryForStudentActivity" /> <!-- NEW -->

        <!-- ✅ Student Features -->
        <activity android:name=".StudentDashboard" />
        <activity android:name=".StudentRegisterActivity" />
        <activity android:name=".AttendanceHistoryActivity" />
        <activity android:name=".EbookListActivity" />
        <activity android:name=".SavedEbooksActivity" />
        <activity android:name=".EditStudentProfileActivity" />
        <activity android:name=".ViewAnnouncementsActivity" />

        <!-- ✅ Login Pages -->
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".SuperAdminLoginActivity" />
        <activity android:name=".StudentLoginActivity" />

        <!-- ✅ Utility Activities -->
        <activity 
            android:name=".QRScannerActivity" 
            android:screenOrientation="portrait"
            tools:replace="screenOrientation"/>

        <!-- ✅ Firebase Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
